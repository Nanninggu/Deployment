package com.dev.example.devdemo.util;

import com.dev.example.devdemo.dto.RoleDto;

public class DivisionValues {

    public static boolean divisionValues(String department_id) {
        String str2 = "aa";
        String str1 = department_id;

        boolean valueCheck = (str1.toUpperCase().contains(str1.toUpperCase()) && str1.length() > 6);
        if (valueCheck = false) {
        }
        return false;
    }
}